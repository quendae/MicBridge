[workspace]
resolver = "2"
members = [
    "crates/mb-i18n",
    "crates/mb-proto",
    "crates/mb-audio",
    "crates/mb-engine",
    "crates/mb-net",
    "crates/mb-app",
    "crates/mb-gui",
]

[workspace.package]
version = "0.1.1"
edition = "2021"
# Podnosi je eframe; reszta zbudowałaby się na starszym.
rust-version = "1.85"
license = "MIT OR Apache-2.0"
repository = "https://github.com/quendae/MicBridge"

[workspace.dependencies]
anyhow = "1.0"
thiserror = "1.0"
serde = { version = "1.0", features = ["derive"] }
ciborium = "0.2"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
cpal = "0.15"
clap = { version = "4.5", features = ["derive"] }
ringbuf = "0.4"

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
